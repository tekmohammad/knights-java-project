package tek.week_8.day_4;

public class Interface {
	
	/*
	 *  An interface in Java looks similar to a class but both the interface 
	 *  and class are two different concepts.
	 *  An interface can have methods and variables just like the 
	 *  class but the methods declared in interface are by default abstract. 
	 *  We can achieve 100% abstraction and multiple inheritance in Java
	 *  with Interface.
	 */ 
	
	// The method in the interfaces are going to be abstract by default.
	// The fields (variables) are going to be final and static in the 
	// interfaces.
	
	// Class -> Class = extends
	// Class -> Interface = implements
	// Inteface -> Inteface = extends
}
