package tek.week_8.day_2;

public class MethodOverriding {
	// Method Overriding (Runtime), (Dynaimic Binding):
	/*
	 * Declaring a method in child class which is already present in the 
	 * parent class is called Method Overriding. In simple words, 
	 * overriding means to override the functionality of an existing method. 
	 * In this case, if we call the method with child class object, 
	 * then the child class method is called. To call the parent class 
	 * method we have to use super keyword.
	 */
}
