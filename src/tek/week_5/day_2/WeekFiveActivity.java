package tek.week_5.day_2;

public class WeekFiveActivity {
	
	/* Question No.1: 
	 * Create two instance variables and then try to print its value
	 * without intitalizing them.
	 */
	
	/* Question No.2: 
	 * Create two Local variables and then try to print its value
	 * without intitalizing them.
	 */
	
	/* Question No.3: 
	 * Create two methods, the method should not accept any parameters
	 * and the first method should be printing "Hello World" in the console.
	 * 
	 * And the second method should print an boolean based on the condition
	 * of your choice. Example: if the condition is true -> true
	 * if the condition is false -> false
	 */
	
	/* Question No.4: 
	 * Create two methods, the method should accept any parameters of your
	 * choice, do the sum and return it's value
	 * Then call this method in the main method and then print it's result
	 * 
	 * The second method, should accept two numbers as input (parameters)
	 * and the do the multiplication and then return the reult.
	 */
	
	/* Question No.5: 
	 * Create a method that will print your firstName and lastName side by side
	 * You have to passs the firstName and the lastName when calling the method.
	 * 
	 * Example output: Welcome Alex Cureto!
	 * Where Alex is the firstName 
	 * and
	 * Cureto is the lastName
	 */
	
	/* Question No.5: 
	 * Create a method that will generate a random number and print the
	 * random number into the console every time you're running your 
	 * program.
	 */
	
	/* Question No.6: 
	 * Create a method that can accept 1D array of String and will print
	 * it's values in the console.
	 */
	

	/* Question No.7: 
	 * Create a method that can accept 2D array of String or int and will print
	 * it's values in the console. or maybe both if you want!
	 */
	
	/* Question No.8: 
	 * Create two methods!
	 * First method should take the user input and add the sum of the values
	 * let's say num1 and num2 -> return num1 + num2
	 * In the second method use the result of first method 
	 */
	
	/* Question No.9: 
	 * Create two methods, the first method should get user input
	 * and the second method should do all of the math operation to them
	 * 
	 * First method -> num1, num2 -> if num1 = 10 and num2 = 20;
	 * then 
	 * Second method should return:
	 * 10 + 20 = 30;
	 * 10 * 20 = X;
	 * 10 / 20 = X;
	 * 10 - 20 = X
	 * and so on
	 * Where X is the result of your calculation.
	 */
	
	/* Question No.10: 
	 * Create a method and define the Local and Instance variables with 
	 * Comments and explain the scope of each variable insidle of that method.
	 * 
	 * YOUR METHOD IS NOT RETURNING ANYTHING!
	 */
	
	/* Question No.11: 
	 * Create a method that will take the start range of number
	 * and also the end range of the numbers.
	 * 
	 * and then print the even numbers between those range
	 * call the method in main method and get the result!
	 */
}
